# Dashboard Language Renderer

Incremental implementation of a Dashboard Language validator and presenter under `pages/dashboard/`.

## Scripts

- `npm install`
- `npm run typecheck`
- `npm run lint`
- `npm run test`
- `npm run test:e2e`

This directory is intentionally self-contained and uses JavaScript ESM with TypeScript `checkJs`.
